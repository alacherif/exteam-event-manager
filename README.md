# exteam-event-manager

install jdk 1.8
install eclipse JEE developpers
install GIT
install node (last version)
install angular-cli
install postgresql (last version)

Data Base configuration is done by default just try to add some data in your tables.
